// eslint-disable-next-line
const colors = require('tailwindcss/colors');

module.exports = {
  theme: {
    colors: {
      white: colors.white,
      teal: colors.teal,
      gray: '#1a202c',
      trueGray: colors.trueGray,
    },
  },
};
